// Vercel exit 0 skips deployment. Remove this gate only after the hosted API is ready.
console.log('Irrint: versão local. Publicação web aguardando API hospedada; manter deployment existente.');
process.exit(0);
